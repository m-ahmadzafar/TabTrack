// content.js
// let metadata = {
//   category: 'Other', // Default category
//   title: document.title || '',
// };

// // Extract Open Graph metadata
// let ogType = document.querySelector('meta[property="og:type"]');
// if (ogType) {
//   if (ogType.content.includes('website') || ogType.content.includes('article')) {
//     metadata.category = 'News'; // Example: If Open Graph type indicates news or article
//   } else if (ogType.content.includes('video')) {
//     metadata.category = 'Entertainment'; // Example: If Open Graph type indicates video
//   }
// }

// // Extract other metadata or keywords from the page to determine category
// // Example: Check keywords in title, URL, or other meta tags

// metadata;
